# Hanzo SQL - PostgreSQL configuration tuned for AI workloads
# https://github.com/hanzoai/sql

# Connection
listen_addresses = '*'
port = 5432
max_connections = 200

# Memory - tuned for vector operations and large working sets
shared_buffers = 512MB
work_mem = 64MB
maintenance_work_mem = 256MB
effective_cache_size = 1536MB

# WAL
wal_level = replica
max_wal_size = 2GB
min_wal_size = 256MB
checkpoint_completion_target = 0.9

# Query planner - favor index scans for vector similarity
random_page_cost = 1.1
effective_io_concurrency = 200
default_statistics_target = 100

# Parallel queries - leverage multi-core for vector ops
max_parallel_workers_per_gather = 4
max_parallel_workers = 8
max_parallel_maintenance_workers = 4
max_worker_processes = 16

# Logging
log_min_duration_statement = 1000
log_checkpoints = on
log_connections = off
log_disconnections = off
log_lock_waits = on
log_timezone = 'UTC'

# Locale
timezone = 'UTC'
lc_messages = 'en_US.utf8'
lc_monetary = 'en_US.utf8'
lc_numeric = 'en_US.utf8'
lc_time = 'en_US.utf8'

# Extensions
# pg_documentdb_core and pg_documentdb must be in shared_preload_libraries;
# pg_documentdb_extended_rum_core is loaded at runtime by pg_documentdb_core
shared_preload_libraries = 'vector,pg_cron,pg_documentdb_core,pg_documentdb'

# pg_cron - run jobs in any database (not just postgres)
cron.database_name = 'postgres'

# pgvector specific - HNSW index build parallelism
max_parallel_maintenance_workers = 4

# pg_documentdb - RUM library load option (default for PG18: require_documentdb_extended_rum)
# documentdb.rum_library_load_option = 'require_documentdb_extended_rum'
