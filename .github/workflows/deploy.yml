name: Docker
# Neutralized: image build/publish moved to the native Hanzo pipeline.
# Canonical deploy: .hanzo/workflows/deploy.yml (act_runner + BuildKit → ghcr.io/hanzoai/sql → operator).
# GitHub is a mirror — this workflow no longer builds or pushes anything.
on:
  workflow_dispatch:
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "native pipeline is .hanzo/workflows/deploy.yml; GitHub is a mirror"
